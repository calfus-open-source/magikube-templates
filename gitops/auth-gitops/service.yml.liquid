---
kind: Service
apiVersion: v1
metadata:
  namespace: {{ projectname }}-{{ environment }}-app
  name: auth-service-svc
  labels:
    app: auth-service
    service: auth-service-svc
spec:
  selector:
    app: auth-service
  type: ClusterIP
  ports:
  - name: http-app
    protocol: TCP
    port: 3000
    targetPort: 3000
....
