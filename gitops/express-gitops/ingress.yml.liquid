apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: node-express-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: express.{{DOMAIN_NAME}}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: node-express-service
            port:
              number: 80
  tls:
  - hosts:
    - express.{{DOMAIN_NAME}}
    secretName: tls-secret
