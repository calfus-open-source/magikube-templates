apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reactjs-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: reactjs.{{DOMAIN_NAME}}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: reactjs-service
            port:
              number: 80
  tls:
  - hosts:
    - reactjs.{{DOMAIN_NAME}}
    secretName: tls-secret
